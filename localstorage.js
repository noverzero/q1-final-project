document.addEventListener("DOMContentLoaded", function(){

  console.log("developed by undefinedindustries.com")
  localStorage.setItem("array" , JSON.stringify([1,2,5,4]))
  let myData = JSON.parse(localStorage.getItem("array"))
  console.log(myData)

//fiurst get the item from local localStorage
//then push onto that arrauy
//setitem back into local storage


let list = []


//get the values from the form
//make an object that contains all of those values
//store them in localstorage using setItem... make sure to stringify

//store the value that signifies the colunn
//write a function that takes sort parameter and outputs the correctly sorted table


//--------------------------
//pass the sort function a column as a parameter::
function sortTable(column){

}
})
